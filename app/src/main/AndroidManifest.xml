<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="AlbumGallery"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlbumGallery"
        tools:targetApi="31"
        android:requestLegacyExternalStorage="true"
        >
        <activity android:name=".view.activity.HomeScreen"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".view.activity.AlbumMain" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="AlbumGallery"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.EditImageActivity" />
        <activity android:name=".view.activity.DetailPicture" />
        <activity android:name=".view.activity.AlbumActivity" />
        <activity android:name=".view.activity.LoginScreen" />
        <activity android:name=".view.activity.RegisterScreen" />
        <activity android:name=".view.activity.CreateAlbumActivity" />
        <activity android:name=".view.activity.MainFragmentController" />

    </application>
</manifest>